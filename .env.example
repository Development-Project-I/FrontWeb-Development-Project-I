VITE_APP_NAME=GastroPlan
# Proxy local/produção → https://gastroplan-api.onrender.com (evita CORS no navegador).
# Use /api (padrão). Só mude se o backend liberar CORS para seu domínio.
VITE_BASE_URL_API=/api
VITE_PRIMARY_COLOR=#165dfc
VITE_SECONDARY_COLOR=#eff6ff
